# CLAUDE.md

Claude Code adapter for the Job Application Agent — the canonical manual is `AGENTS.md`.
In Claude Code the routines are available as slash commands: `/setup`, `/use`,
`/search`, `/apply`, `/report` (see `.claude/commands/`).

@AGENTS.md
